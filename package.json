{
  "name": "talq",
  "description": "A communication platform built with React and GraphQL",
  "version": "0.1.0",
  "license": "MIT",
  "private": true,
  "homepage": "https://github.com/joealden/talq",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/joealden/talq.git"
  },
  "scripts": {
    "client:build": "cd client; yarn build",
    "client:dev": "cd client; yarn dev",
    "client:clean": "./scripts/clean.sh client",
    "client:install": "yarn client:clean; cd client; yarn",
    "client:start": "cd client; yarn start",
    "api:clean": "./scripts/clean.sh api",
    "api:install": "yarn api:clean; cd api; yarn",
    "api:prisma:deploy": "cd api; yarn prisma:deploy"
  }
}
